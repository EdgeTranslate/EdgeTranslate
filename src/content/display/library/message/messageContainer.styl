/**
 * export to select.styl
 */
#edge-translate-message-container {
	display: flex;
	flex-direction: column;
	justify-content: flex-start;
	align-items: center;
	position: fixed;
	top: 0;
	z-index: 10000001;
	pointer-events: none;
}

.edge-translate-message-center {
	left: 0;
	right: 0;
}

.edge-translate-message-left {
	left: 5vw;
}

.edge-translate-message-right {
	right: 5vw;
}
