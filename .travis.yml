language: node_js
node_js:
    - "10"
cache:
    directories:
        - "node_modules"
script:
    - cd packages/EdgeTranslate && npm run build:chrome
    - cd packages/EdgeTranslate && npm run build:firefox
    - cd packages/EdgeTranslate && npm run pack:chrome
    - cd packages/EdgeTranslate && npm run pack:firefox
